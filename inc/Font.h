#ifndef FONT_H
#define FONT_H

#include <SDL2/SDL.h>
// I hate to load 50000000 times the same font, so there it was.

const SDL_Color WHITE = { 255, 255, 255, 255 };
const SDL_Color BLACK = { 0, 0, 0, 255 };


#endif
